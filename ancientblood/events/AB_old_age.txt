namespace = old_age

old_age.1 = { #test to add a modifier
    type = country_event
    title = "old_age.1"
    desc = "old_age.1"

    right_portrait = scope:old_person_first
    right_portrait = scope:old_person_second
    right_portrait = scope:old_person_third

    immediate = {
        ordered_character = {
            limit = {
                is_alive = yes
                age > 80
            }
            order_by = age
            max = 3
            if = {
                limit = {
                    NOT = {
                        exists = scope:old_person_first
                    }
                }
                save_scope_as = old_person_first
            }
            else_if = {
                limit = {
                    NOT = {
                        exists = scope:old_person_second
                    }
                }
                save_scope_as = old_person_second
            }
            else_if = {
                limit = {
                    NOT = {
                        exists = scope:old_person_third
                    }
                }
                save_scope_as = old_person_third
            }    
        }        
    }
    option = {
        name = "old_age.1.a"
    }

}